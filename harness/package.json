{
  "name": "zpos-kasir-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Verification harness ZPos Kasir — deterministic Playwright Chromium against mocked __TAURI__ + SQLite fixture",
  "scripts": {
    "test": "node run.mjs",
    "shots": "node run.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
